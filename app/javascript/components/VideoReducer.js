import {
    ADD_VIDEO,
    DELETE_VIDEO

} from './UploadVideoActions'

export default cost reducer = (state = {}, action) => {
    switch(action) {
        case
        case ADD_VIDEO:
            return Object.assign({}, state, {

            })
    }
}