import {
    ADD_VIDEO,
    DELETE_VIDEO
} from './VideoActions'

export default cost reducer = (state = {}, action) => {
    switch(action) {
        case ADD_VIDEO:
            return Object.assign({}, state, {

            })
    }
}